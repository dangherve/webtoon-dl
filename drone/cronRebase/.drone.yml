---
kind: pipeline
type: exec
name: cronRebase

platform:
  os: linux
  arch: amd64

trigger:
    branch:
        include:
            - master

steps:
  - name: sync repo
    commands:
      - ./buildTools/rebase.sh

node:
  internet: high
